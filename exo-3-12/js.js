
document.addEventListener('DOMContentLoaded', () => {

    const monContent = document.querySelector('.main-content');

    const styles = getComputedStyle(monContent);
    console.log({styles});


    // monContent.style.backgroundColor = "red";
    // console.log({bg: monContent.style.backgroundColor});
    // monContent.dataset.name = "monBababouillomenton";

});
